apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-kpw-aks-demo
  labels:
    app: app-kpw-aks-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-kpw-aks-demo
  template:
    metadata:
      labels:
        app: app-kpw-aks-demo
    spec:
      containers:
        - name: app
          image: ghcr.io/natthakritc/app-kpw-aks-demo:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: app-kpw-aks-demo
spec:
  selector:
    app: app-kpw-aks-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-kpw-aks-demo
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  rules:
    - http:
        paths:
          - path: /app1
            pathType: Prefix
            backend:
              service:
                name: app-kpw-aks-demo
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app-kpw-aks-demo
                port:
                  number: 80
